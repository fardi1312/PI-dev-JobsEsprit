<form method="post" action="{{ path('app_covoiturage_deleteAdminCovoiturage', {'id': covoiturage.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ covoiturage.id) }}">
    <button class="btn">Delete</button>
</form>
